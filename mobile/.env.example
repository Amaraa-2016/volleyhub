# Backend base URL used by the app. Leave unset in development and the app derives it from the
# packager host, which is what a phone on the same wifi needs.
EXPO_PUBLIC_API_URL=http://localhost:5090
