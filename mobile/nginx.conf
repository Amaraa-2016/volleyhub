server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # expo-router produces client-side routes; unknown paths must still serve the app shell.
    location / {
        try_files $uri $uri.html $uri/ /index.html;
    }
}
